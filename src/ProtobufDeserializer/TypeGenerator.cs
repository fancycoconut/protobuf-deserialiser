using System;
using System.Collections.Generic;
using System.Text;

namespace ProtobufDeserializer
{
    /// <summary>
    /// This won't be an option if we are targeting against NET Standard 2.0 because it does not support dynamic assembly
    /// </summary>
    public class TypeGenerator
    {
    }
}
